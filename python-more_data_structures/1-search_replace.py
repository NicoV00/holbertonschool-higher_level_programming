#!/usr/bin/python3

def search_replace(my_list, search, replace):
    new_list = []
    for o in range(len(my_list)):
        new_list.append(my_list[o])
        if new_list[o] == search: . 
            new_list[o] = replace
    return (new_list)
